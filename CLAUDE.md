# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

NutriTrack 2.0 is a nutrition-tracking mobile app targeting Vietnamese Gen Z/Millennials. It uses React Native (Expo) for the frontend and AWS Amplify Gen 2 for the backend. The app features food photo/voice logging with AI analysis (Qwen3-VL on AWS Bedrock), an AI coach persona ("Bảo"), gamification (streaks, pet evolution, challenges), and a bilingual UI (Vietnamese/English).

## Project Structure

```
NutriTrack/
├── frontend/              # Expo React Native app
│   ├── app/               # Expo Router screens
│   ├── src/               # Stores, services, components, constants
│   ├── assets/            # Images, fonts
│   ├── MANHINH/           # Pet evolution videos (1.mp4–5.mp4)
│   ├── amplify_outputs.json  # Auto-generated by pipeline-deploy (do not edit)
│   └── package.json
├── backend/               # AWS Amplify Gen 2 + FastAPI
│   ├── amplify/           # Amplify backend (source of truth)
│   │   ├── backend.ts     # All AWS resources defined here
│   │   ├── data/          # GraphQL schema (resource.ts)
│   │   ├── ai-engine/     # Lambda: AI multi-action handler
│   │   ├── friend-request/ # Lambda: friend system
│   │   ├── process-nutrition/ # Lambda: nutrition lookup
│   │   ├── resize-image/  # Lambda: S3 image trigger
│   │   ├── auth/          # Cognito config
│   │   ├── storage/       # S3 config
│   │   └── package.json
│   ├── main.py            # FastAPI app entry
│   ├── routes/            # FastAPI routes
│   └── requirements.txt
├── infrastructure/        # Terraform for ECS Fargate (optional deploy)
├── ECS/                   # Docker + CI/CD for ECS deployment
├── amplify/               # LEGACY — stub directories only, NOT used
└── amplify.yml            # CI/CD build config (Amplify Hosting)
```

**Important**: The root `amplify/` directory is a legacy stub from before restructure. All active Amplify backend code lives in `backend/amplify/`.

## Commands

```bash
# Frontend (run from frontend/)
cd frontend
npm install --legacy-peer-deps
npm start              # Start Expo dev server
npm run android        # Run on Android
npm run ios            # Run on iOS
npm run web            # Run on web
npm run build          # Export web build (frontend/dist/)

# Testing (run from frontend/)
cd frontend
npm test               # Run all tests (jest-expo)
npm run test:watch     # Watch mode
npm run test:coverage  # Coverage report (80% threshold on src/**)
npm run test:pbt       # Property-based tests only (fast-check)
npx jest path/to/file  # Run a single test file

# Amplify backend (run from backend/)
cd backend
npx ampx sandbox                                          # Start local sandbox
npx ampx generate outputs --outputs-out-dir ../frontend  # Regenerate amplify_outputs.json
npx ampx sandbox secret list                             # List sandbox secrets
npx ampx sandbox secret set KEY                          # Set a sandbox secret
npx ampx sandbox delete                                  # Delete sandbox resources
```

Install frontend with `npm install --legacy-peer-deps` (required due to peer dep conflicts; enforced in `frontend/.npmrc`).

Each Lambda function (`backend/amplify/ai-engine/`, `backend/amplify/process-nutrition/`, etc.) has its own `package.json` and must be installed separately (see `amplify.yml` build phases).

## Architecture

### Frontend (Expo Router file-based routing)

- **`frontend/app/_layout.tsx`** — Root layout: wraps everything in `LanguageProvider` + `GestureHandlerRootView`, handles auth guard (redirects unauthenticated users to `/welcome`), biometric prompt, and Android navigation bar setup.
- **`frontend/app/(tabs)/`** — Main tab screens: `home`, `kitchen`, `battle`, `ai-coach`, `progress`, `add` (center "+" button).
- **`frontend/app/`** — Top-level stack screens: auth flow (`welcome`, `login`, `signup`, `verify-otp`), onboarding, profile editing, recipe flow, food detail, settings, etc.

### State Management (Zustand)

All stores in `frontend/src/store/`: `authStore`, `userStore`, `foodStore`, `mealStore`, `fridgeStore`, `settingsStore`. Accessed via hooks (`useAuthStore`, etc.). Currently all stores use AsyncStorage for local persistence — DynamoDB sync is being added (see data models below).

### Services (`frontend/src/services/`)

- `authService.ts` — AWS Amplify Auth (Cognito) with Google OAuth, session management via SecureStore
- `aiService.ts` — Unified AI service calling `aiEngine` Lambda via Amplify GraphQL. Supports 10 actions: analyzeFoodImage, generateCoachResponse, searchFoodNutrition, fixFood, voiceToFood, ollieCoachTip, generateRecipe, calculateMacros, challengeSummary, weeklyInsight. Parses responses via `extractAndParseJSON()` with code block fallback.
- `audioService.ts` — Voice recording for food logging
- `userService.ts` — User profile CRUD via Amplify Data, `syncOnboardingWithDB()` called on login
- `mealService.ts` — Meal/food log CRUD via Amplify Data
- `fridgeService.ts` — Fridge inventory CRUD via Amplify Data

### Backend (AWS Amplify Gen 2)

- **`backend/amplify/backend.ts`** — Defines all backend resources: auth, data (GraphQL/DynamoDB), storage (S3), and Lambda functions. Configures IAM policies for Bedrock (Qwen3-VL in ap-southeast-2), S3 access, and Transcribe.
- **`backend/amplify/data/resource.ts`** — GraphQL schema with models:
  - `Food` — Nutrition database (~200 Vietnamese food items)
  - `user` — Profile, biometrics, goals, gamification, ai_preferences, friend_code
  - `FoodLog` — Meal history with date GSI (owner-scoped)
  - `FridgeItem` — Fridge inventory (owner-scoped)
  - `Challenge` + `ChallengeParticipant` — Group challenges (hasMany/belongsTo)
  - `Friendship` — Bidirectional friend records (owner-scoped, friend_id GSI)
  - `UserPublicStats` — Leaderboard stats (owner write, authenticated read)
  - Queries: `aiEngine`, `processNutrition`
  - Mutation: `friendRequest`
- **Lambda functions (in `backend/amplify/`):**
  - `ai-engine/` — Qwen3-VL (qwen.qwen3-vl-235b-a22b) multi-action handler with 10 actions and integrated prompt templates
  - `process-nutrition/` — Hybrid nutrition lookup: DynamoDB fuzzy match first, AI fallback
  - `friend-request/` — Friend system: send/accept/decline/remove requests, search by friend code
  - `resize-image/` — S3 trigger for image processing on `incoming/` prefix
- **Storage:** S3 bucket with paths: `incoming/{entity_id}/*` (image upload), `voice/{entity_id}/*` (voice recordings), `media/{entity_id}/*` (processed assets)

### AWS Environments

There are 3 active backend environments, each with its own DynamoDB tables:

| Environment | Lambda prefix | Table suffix | Notes |
|-------------|---------------|--------------|-------|
| **Sandbox** (local dev) | `amplify-nutritrackbackend-tdtp2--` | `tynb5fej6jeppdrgxizfiv4l3m` | Run via `npx ampx sandbox` from `backend/`; stack: `amplify-nutritrackbackend-tdtp2-sandbox-390e5e2cb5` |
| **Branch: feat/phase3** | `amplify-d1glc6vvop0xlb-fe-` | `vic4ri35gbfpvnw5nw3lkyapki` | Deploy via Amplify CI on push |
| **Branch: main** | `amplify-d1glc6vvop0xlb-ma-` | `2c73cq2usbfgvp7eaihsupyjwe` | Deploy via Amplify CI on push |

`amplify_outputs.json` in `frontend/` is auto-generated per environment — do not edit manually. Always run `npx ampx generate outputs --outputs-out-dir ../frontend` from `backend/` after backend changes.

### Key Patterns

- **AI model**: All AI calls use Qwen3-VL on AWS Bedrock (ap-southeast-2). No Gemini/Claude dependency. Response format: `choices[0].message.content` with fallback chain.
- **i18n**: `frontend/src/i18n/LanguageProvider.tsx` + `translations.ts` — Vietnamese/English with `useAppLanguage()` hook providing `t()` function.
- **Path aliases**: `@/*` maps to `frontend/` root (configured in `frontend/tsconfig.json`).
- **Amplify config**: Initialized in `frontend/src/lib/amplify.ts`, imported as side-effect in root layout.
- **Security layer**: `frontend/src/security/` includes biometric auth, screen protection, input validation, secure storage, session management, and error boundaries.
- **Design tokens**: Colors in `frontend/src/constants/colors.ts`, typography in `frontend/src/constants/typography.ts`. Primary: Dark Navy (#1B2838), Accent: Green (#2ECC71).
- **Auth flow**: Cognito email+password with OTP email verification on signup. Google OAuth via `signInWithRedirect`. Auth guard in `_layout.tsx` redirects unauthenticated users to `/welcome`. Login handles `UserNotFoundException` (→ signup), `UserNotConfirmedException` (→ resend OTP + verify-otp), and `NotAuthorizedException` (wrong password).
- **Amplify backend IAM**: `backend.ts` uses CDK escape hatch (`backend.aiEngine.resources.lambda.addToRolePolicy`) for Bedrock/S3/Transcribe IAM policies — `addEnvironment` does not work on `IFunction`.
- **Known issue — `discoverTables()` in friend-request Lambda**: Lambda uses dynamic table discovery which picks wrong table when multiple environments exist. Fix: pass table name via env var in `backend/amplify/backend.ts`.

### Testing

Tests live in `frontend/src/__tests__/` using jest-expo preset. Property-based tests use `fast-check` (matched by test name pattern `Property:`). `frontend/jest.setup.js` mocks TurboModuleRegistry, expo-secure-store, AsyncStorage, biometric APIs, and screen capture — check this file when adding new native module dependencies.
